# Ollama Cloud Engine Configuration
# Copy this file to vars.env and customize for your deployment

# REQUIRED VARIABLES
CLOUD=aws                                    # aws or gcp
TF_VAR_tailscale_auth_key=tskey-auth-xxx...  # Your Tailscale auth key
TF_VAR_model_choice=codellama:7b-code        # Model to deploy

# OPTIONAL VARIABLES (uncomment and modify if needed)
# TF_VAR_instance_name=Ollama-LLM-Server     # Instance name
# TF_VAR_enable_debug=false                  # Debug logging

# AWS Optional (if CLOUD=aws)
# TF_VAR_aws_profile=default                 # AWS profile name
# TF_VAR_aws_region=us-east-1                # AWS region
# TF_VAR_custom_ami_id=ami-xxx               # Override AMI selection

# GCP Optional (if CLOUD=gcp)
# TF_VAR_gcp_project=your-project-id        # Uses gcloud default if not set
# TF_VAR_gcp_region=us-central1              # GCP region
# TF_VAR_gcp_zone=us-central1-a              # GCP zone